@page
@model CinemaProject.Pages.Articles.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Article[0].Content)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Article[0].Date)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Article[0].WorkerWorker)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Article) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Content)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Date)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.WorkerWorker.Name)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ArticleId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ArticleId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ArticleId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
