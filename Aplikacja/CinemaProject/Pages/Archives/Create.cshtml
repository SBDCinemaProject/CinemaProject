@page
@model CinemaProject.Pages.Archives.CreateModel

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Archive</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Archive.UserUserId" class="control-label"></label>
                <select asp-for="Archive.UserUserId" class ="form-control" asp-items="ViewBag.UserUserId"></select>
            </div>
            <div class="form-group">
                <label asp-for="Archive.ScreeningScreeningId" class="control-label"></label>
                <select asp-for="Archive.ScreeningScreeningId" class ="form-control" asp-items="ViewBag.ScreeningScreeningId"></select>
            </div>
            <div class="form-group">
                <label asp-for="Archive.Date" class="control-label"></label>
                <input asp-for="Archive.Date" class="form-control" />
                <span asp-validation-for="Archive.Date" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
