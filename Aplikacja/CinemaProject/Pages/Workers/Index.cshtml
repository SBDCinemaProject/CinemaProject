@page
@model CinemaProject.Pages.Workers.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Worker[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Worker[0].Surname)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Worker[0].Salary)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Worker[0].Position)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Worker[0].CinemaCinema.Name)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Worker) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Surname)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Salary)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Position)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CinemaCinema.Name)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.WorkerId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.WorkerId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.WorkerId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
