@page
@model CinemaProject.Pages.Directors.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Director[0].Firstname)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Director[0].Lastname)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Director[0].Birthday)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Director[0].Nationality)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Director) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Firstname)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Lastname)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Birthday)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Nationality)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.DirectorId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.DirectorId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.DirectorId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
